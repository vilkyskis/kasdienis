<nav class="navbar navbar-expand-sm navbar-dark" style="background-color: #6E6AEE !important;"  data-spy="affix" data-offset-top="197">

    <a href="/" class="navbar-brand">Kasdienis</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
        <span class="navbar-toggle-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mobile-nav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="/" class="nav-link">Home</a>
            </li>
            {% if is_granted('ROLE_USER')%}
                <li class="nav-item">
                    <a href="{{ path('post_index') }}" class="nav-link">Posts</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('topic_index') }}" class="nav-link">Topic</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('fos_user_security_logout') }}" class="nav-link btn btn-danger navbar-btn text-white">Logout</a>
                </li>
            {% else %}

            <li class="nav-item"><a href="{{ path('fos_user_security_login') }}" class="nav-link"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
            <li class="nav-item">
                <a href="{{ path('fos_user_registration_register') }}" class="nav-link btn btn-danger navbar-btn text-white">Register</a>
            </li>
            {% endif %}
            {#{% if is_granted('ROLE_ADMIN')%}

            {% endif %}#}
        </ul>
    </div>

</nav>