{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    {#<table class="table">
        <tbody>
            <tr>
                <th><b>Title</b></th>
                <td><b>{{ post.Title }}</b></td>
            </tr>
            <tr>
                <th><h9>Date</h9></th>
                <td><h5>{{ post.Date ? post.Date|date('Y-m-d H:i:s') : '' }}</h5></td>
            </tr>
            <tr>
                <th>Turinys</th>
                <td>{{ post.Data }}</td>
            </tr>
            <tr>
                <th>Autorius</th>
                <td>{{ post.author }}</td>
            </tr>
            <tr>
                <th>Upvotes</th>
                <td>{{ post.upvotes }}</td>
            </tr>
        </tbody>
    </table>#}

    <div class="container bg-secondary text-white">

        <h1><b>{{ post.Title }}</b></h1>
        <h6>{{ post.Date ? post.Date|date('Y-m-d H:i:s') : '' }}</h6>
        <p>{{ post.Data }}</p>
        <h6 class="small">{{ post.upvotes }} upvotes</h6>
    </div>

    <a href="{{ path('post_index') }}">back to list</a>

    <a href="{{ path('post_upvote', {'id': post.id}) }}">Upvote</a>

    <a class="btn btn-warning" href="{{ path('post_edit', {'id': post.id}) }}">edit</a>

    {#{{ include('post/_delete_form.html.twig') }}#}
{% endblock %}
